# DEPRECATED: This repository is using a GitHub-only admin page that links to GitHub's web editor.
# The previous Netlify CMS configuration is intentionally disabled to avoid external dependencies.
# If you later want to re-enable a JS-based in-browser CMS, replace this file with a valid config.
